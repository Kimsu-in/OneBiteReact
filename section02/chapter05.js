// 원시타입과 객체타입은 값이 저장되거나 복사되는 과정이 서로 다르다

// 원시 타입(Number, String, Boolean 등)
// 값 자체로써 변수에 저장되고 복사된다.
// 값이 변경되도 메모리엔 그대로 남아있음. = 불변값

// 객체 타입(Object, Array, Function 등)
// 참조값을 통해 변수에 저장되고 복사된다.
// 원본 데이터 메모리 값를 수정합 = 가변값

// 객체(object)
// 함수(Function)과 배열(Array)로 구성
// 함수: 호출, 선언, ...
// 배열: 순차 저장, 순회, ...
